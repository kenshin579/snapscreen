# 펜(자유곡선) 도구 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 편집기에 손으로 그리는 자유곡선 펜 주석을 추가한다(그리기·부드러운 곡선 렌더·선택/이동/삭제·저장 반영).

**Architecture:** `AnnotationKind.path([CGPoint])`로 raw 점열을 저장하고, 렌더 시에만 `PathSmoother`(순수 함수, 이차 베지어 중점 스무딩)로 곡선화한다. 입력은 `CanvasView`의 별도 상태(`penPoints`)로 누적(crop의 `isCropping` 패턴). 색·굵기·undo·선택은 기존 공용 경로 재사용.

**Tech Stack:** Swift, CoreGraphics(CGMutablePath), AppKit(CanvasView), XCTest.

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `Editor/PathSmoother.swift` | 점열 → 스무딩된 `CGPath` 순수 함수 | 신규 |
| `Editor/Annotation.swift` | `AnnotationKind.path` 케이스 + translated/bounds | 수정 |
| `Editor/AnnotationRenderer.swift` | `path` 렌더(스무딩 stroke) | 수정 |
| `Editor/AnnotationHitTester.swift` | `path` 선분 근접 히트 | 수정 |
| `Editor/EditorState.swift` | `EditorTool.pen` 등록(label/symbol) | 수정 |
| `Editor/CanvasView.swift` | 펜 입력(점 누적) + 단축키 `p` | 수정 |
| `Tests/SnapScreenKitTests/PathSmootherTests.swift` | 스무딩 단위 테스트 | 신규 |
| `Tests/SnapScreenKitTests/PenAnnotationTests.swift` | 모델/히트 단위 테스트 | 신규 |
| `docs/manual-test-checklist.md`, `Resources/Info.plist`, `Support/AppInfo.swift`, `README.md`, `CLAUDE.md` | 문서·버전 | 수정 |

---

### Task 1: PathSmoother (순수 함수, TDD)

**Files:**
- Create: `Sources/SnapScreenKit/Editor/PathSmoother.swift`
- Test: `Tests/SnapScreenKitTests/PathSmootherTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`Tests/SnapScreenKitTests/PathSmootherTests.swift`:

```swift
import XCTest
import CoreGraphics
@testable import SnapScreenKit

final class PathSmootherTests: XCTestCase {
    func testEmptyPointsGivesEmptyPath() {
        XCTAssertTrue(PathSmoother.smoothedPath([]).isEmpty)
    }

    func testSinglePointNotEmpty() {
        let path = PathSmoother.smoothedPath([CGPoint(x: 10, y: 10)])
        XCTAssertFalse(path.isEmpty)
    }

    func testTwoPointsIsStraightLine() {
        let a = CGPoint(x: 0, y: 0), b = CGPoint(x: 100, y: 40)
        let path = PathSmoother.smoothedPath([a, b])
        XCTAssertEqual(path.currentPoint.x, 100, accuracy: 0.01)
        XCTAssertEqual(path.currentPoint.y, 40, accuracy: 0.01)
        // 두 점을 감싸는 bounding box
        let box = path.boundingBoxOfPath
        XCTAssertEqual(box.minX, 0, accuracy: 0.01)
        XCTAssertEqual(box.maxX, 100, accuracy: 0.01)
    }

    func testMultiPointEndsAtLastPoint() {
        let pts = [CGPoint(x: 0, y: 0), CGPoint(x: 50, y: 80),
                   CGPoint(x: 100, y: 0), CGPoint(x: 150, y: 80)]
        let path = PathSmoother.smoothedPath(pts)
        XCTAssertEqual(path.currentPoint.x, 150, accuracy: 0.01)
        XCTAssertEqual(path.currentPoint.y, 80, accuracy: 0.01)
        XCTAssertFalse(path.isEmpty)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter PathSmootherTests`
Expected: 컴파일 실패 (`PathSmoother` 없음).

- [ ] **Step 3: PathSmoother 구현**

`Sources/SnapScreenKit/Editor/PathSmoother.swift`:

```swift
import CoreGraphics

/// 자유곡선 점열을 부드러운 CGPath로 변환하는 순수 함수 (이차 베지어 중점 스무딩).
/// 저장 모델은 raw 점열이고, 이 변환은 렌더 시에만 쓰인다. AppKit 비의존 → 단위 테스트 가능.
public enum PathSmoother {
    public static func smoothedPath(_ points: [CGPoint]) -> CGPath {
        let path = CGMutablePath()
        guard let first = points.first else { return path }
        path.move(to: first)
        if points.count == 1 {
            path.addLine(to: first) // round cap이면 점 하나가 원형 점으로 찍힘
            return path
        }
        if points.count == 2 {
            path.addLine(to: points[1])
            return path
        }
        // 각 내부 점을 control로, 다음 점과의 중점을 곡선 끝점으로
        for i in 1..<(points.count - 1) {
            let control = points[i]
            let mid = CGPoint(x: (points[i].x + points[i + 1].x) / 2,
                              y: (points[i].y + points[i + 1].y) / 2)
            path.addQuadCurve(to: mid, control: control)
        }
        path.addLine(to: points[points.count - 1])
        return path
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test --filter PathSmootherTests`
Expected: 4 tests PASS.

- [ ] **Step 5: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/PathSmoother.swift Tests/SnapScreenKitTests/PathSmootherTests.swift
git commit -m "feat: 자유곡선 스무딩 순수 함수 (이차 베지어 중점)"
```

---

### Task 2: AnnotationKind.path 모델

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/Annotation.swift`
- Test: `Tests/SnapScreenKitTests/PenAnnotationTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`Tests/SnapScreenKitTests/PenAnnotationTests.swift`:

```swift
import XCTest
import CoreGraphics
@testable import SnapScreenKit

final class PenAnnotationTests: XCTestCase {
    func testPathBoundsIsBoundingBox() {
        let kind = AnnotationKind.path([CGPoint(x: 10, y: 20), CGPoint(x: 40, y: 5),
                                        CGPoint(x: 25, y: 60)])
        let b = kind.bounds
        XCTAssertEqual(b.minX, 10, accuracy: 0.01)
        XCTAssertEqual(b.minY, 5, accuracy: 0.01)
        XCTAssertEqual(b.maxX, 40, accuracy: 0.01)
        XCTAssertEqual(b.maxY, 60, accuracy: 0.01)
    }

    func testEmptyPathBoundsIsZero() {
        XCTAssertEqual(AnnotationKind.path([]).bounds, .zero)
    }

    func testPathTranslatedMovesAllPoints() {
        let kind = AnnotationKind.path([CGPoint(x: 0, y: 0), CGPoint(x: 10, y: 10)])
        guard case .path(let pts) = kind.translated(by: CGVector(dx: 5, dy: -3)) else {
            return XCTFail("expected path")
        }
        XCTAssertEqual(pts[0], CGPoint(x: 5, y: -3))
        XCTAssertEqual(pts[1], CGPoint(x: 15, y: 7))
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter PenAnnotationTests`
Expected: 컴파일 실패 (`.path` 케이스 없음).

- [ ] **Step 3: Annotation.swift에 path 케이스 추가**

`AnnotationKind` enum에 케이스 추가 (`case stepBadge...` 다음 줄):

```swift
    case path([CGPoint])
```

`translated(by d:)`의 switch에 케이스 추가 (`case .stepBadge...` 다음):

```swift
        case .path(let pts):
            return .path(pts.map { CGPoint(x: $0.x + d.dx, y: $0.y + d.dy) })
```

`bounds`의 switch에 케이스 추가 (`case .stepBadge...` 다음):

```swift
        case .path(let pts):
            guard let first = pts.first else { return .zero }
            var minX = first.x, minY = first.y, maxX = first.x, maxY = first.y
            for p in pts.dropFirst() {
                minX = min(minX, p.x); minY = min(minY, p.y)
                maxX = max(maxX, p.x); maxY = max(maxY, p.y)
            }
            return CGRect(x: minX, y: minY, width: maxX - minX, height: maxY - minY)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test --filter PenAnnotationTests`
Expected: 3 tests PASS. (이 시점에 `AnnotationRenderer`/`AnnotationHitTester`의 switch가 `.path` 미처리로 **컴파일 에러**가 날 수 있다. 그럴 경우 Task 3·4 완료 전까지 두 파일 switch에 임시로 `case .path: break`를 넣지 말고, Task 순서상 Task 3(렌더)·4(히트)를 먼저 최소 구현해도 된다. 권장: Step 3에서 케이스 추가 후 곧바로 Task 3·4의 구현 케이스까지 넣어 전체 컴파일을 통과시킨 뒤 테스트. 아래 Step 5 커밋은 전체 빌드가 통과한 후 한다.)

- [ ] **Step 5: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/Annotation.swift Tests/SnapScreenKitTests/PenAnnotationTests.swift
git commit -m "feat: AnnotationKind.path 모델 (bounds/translated)"
```

---

### Task 3: 렌더링 (AnnotationRenderer)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/AnnotationRenderer.swift`

- [ ] **Step 1: path 렌더 케이스 추가**

`draw(_ annotation:...)`의 `switch annotation.kind`에 케이스 추가 (`case .stepBadge...` 다음):

```swift
        case .path(let points):
            ctx.setStrokeColor(color)
            ctx.setLineWidth(annotation.lineWidth)
            ctx.setLineCap(.round)
            ctx.setLineJoin(.round)
            ctx.addPath(PathSmoother.smoothedPath(points))
            ctx.strokePath()
```

- [ ] **Step 2: 빌드 확인**

Run: `swift build`
Expected: 성공(경고 0). (Task 4의 히트 케이스가 아직이면 `AnnotationHitTester`에서 컴파일 에러 — Task 4를 함께 진행해 전체 빌드를 통과시킨다.)

- [ ] **Step 3: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/AnnotationRenderer.swift
git commit -m "feat: 펜 획 렌더 (스무딩 stroke)"
```

---

### Task 4: 히트 테스트 (선택·이동용)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/AnnotationHitTester.swift`
- Test: `Tests/SnapScreenKitTests/PenAnnotationTests.swift` (기존 파일에 추가)

- [ ] **Step 1: 실패하는 테스트 추가**

`PenAnnotationTests`에 메서드 추가:

```swift
    func testPathHitOnSegment() {
        let a = Annotation(kind: .path([CGPoint(x: 0, y: 0), CGPoint(x: 100, y: 0)]))
        // 선분 (0,0)-(100,0) 위 근처 점
        XCTAssertNotNil(AnnotationHitTester.hitTest(CGPoint(x: 50, y: 3),
                                                    annotations: [a], tolerance: 8))
    }

    func testPathMissFarAway() {
        let a = Annotation(kind: .path([CGPoint(x: 0, y: 0), CGPoint(x: 100, y: 0)]))
        XCTAssertNil(AnnotationHitTester.hitTest(CGPoint(x: 50, y: 40),
                                                 annotations: [a], tolerance: 8))
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter PenAnnotationTests`
Expected: `.path` 미처리 switch로 컴파일 실패, 또는 (임시 처리 시) 히트 테스트 FAIL.

- [ ] **Step 3: HitTester에 path 케이스 추가**

`hits(_:_:_:)`의 switch에 케이스 추가 (`case .stepBadge...` 다음). 기존 `distanceToSegment` 헬퍼를 재사용해 인접 점 선분마다 거리 판정:

```swift
        case .path(let pts):
            guard pts.count >= 2 else {
                // 점 하나짜리 획: 그 점과의 거리로 판정
                if let only = pts.first { return hypot(p.x - only.x, p.y - only.y) <= tol }
                return false
            }
            for i in 0..<(pts.count - 1) {
                if distanceToSegment(p, pts[i], pts[i + 1]) <= tol { return true }
            }
            return false
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test --filter PenAnnotationTests`
Expected: 5 tests PASS (기존 3 + 히트 2).

- [ ] **Step 5: 전체 빌드/테스트**

Run: `swift build && swift test`
Expected: 빌드 성공(경고 0), 전체 테스트 PASS.

- [ ] **Step 6: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/AnnotationHitTester.swift Tests/SnapScreenKitTests/PenAnnotationTests.swift
git commit -m "feat: 펜 획 히트 테스트 (선분 근접)"
```

---

### Task 5: 도구 등록 + 입력 (CanvasView)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/EditorState.swift`
- Modify: `Sources/SnapScreenKit/Editor/CanvasView.swift`

- [ ] **Step 1: EditorTool.pen 등록**

`EditorState.swift`의 `EditorTool` enum:
- `case arrow, rectangle, ellipse, text, blur, pixelate, stepBadge` → 끝에 `, pen` 추가
- `label` switch에 추가: `case .pen: return "펜"`
- `symbolName` switch에 추가: `case .pen: return "scribble"`

- [ ] **Step 2: CanvasView에 펜 상태 프로퍼티 추가**

`CanvasView`의 crop 상태 프로퍼티 근처(예: `private var cropCancelButton: NSButton?` 다음)에 추가:

```swift
    // 펜 자유곡선 그리기 중 누적 점열 (이미지 픽셀 좌표). nil이면 펜 드로잉 중 아님.
    private var penPoints: [CGPoint]?
```

- [ ] **Step 3: mouseDown에 펜 시작 분기**

`mouseDown`의 `switch state.tool`에서 `default: dragMode = .drawing(start: p)` 를 아래로 교체(펜만 별도 처리, 나머지는 기존 drawing):

```swift
        case .pen:
            penPoints = [p]
            draft = Annotation(kind: .path([p]), color: state.color, lineWidth: defaultLineWidth)
        default:
            dragMode = .drawing(start: p)
```

(주의: `mouseDown`은 이미 crop·hitTest·이미지영역 가드를 통과한 뒤이므로, 기존 획 위를 클릭하면 그 획이 선택되고 여기 도달하지 않는다 — 이동과 새 그리기가 자연스럽게 공존한다.)

- [ ] **Step 4: mouseDragged에 펜 누적 분기**

`mouseDragged` 맨 앞(크롭 분기 `if isCropping, let start = cropStart { ... }` **다음**, `let p = imagePoint(from: event)` **앞**)에 추가:

```swift
        if penPoints != nil {
            let p = imagePoint(from: event)
            penPoints?.append(p)
            if let pts = penPoints { draft?.kind = .path(pts) }
            needsDisplay = true
            return
        }
```

- [ ] **Step 5: mouseUp에 펜 확정 분기**

`mouseUp` 맨 앞(크롭 분기 `if isCropping { ... }` **다음**, `switch dragMode` **앞**)에 추가:

```swift
        if let pts = penPoints {
            if pts.count >= 2, let draft { store.add(draft) }
            penPoints = nil
            draft = nil
            needsDisplay = true
            return
        }
```

- [ ] **Step 6: 단축키 `p` 추가**

`keyDown`의 `mapping` 딕셔너리에 `"p": .pen` 추가:

```swift
            let mapping: [String: EditorTool] = [
                "a": .arrow, "r": .rectangle, "o": .ellipse,
                "t": .text, "g": .blur, "b": .pixelate, "n": .stepBadge,
                "p": .pen
            ]
```

- [ ] **Step 7: 빌드 + 로컬 실행 확인**

Run: `swift build 2>&1 | grep -ci warning` → 0
Run: `Scripts/run.sh` → 앱 실행 후 펜 도구로 그려보기(수동). (자동 테스트 불가 영역)

- [ ] **Step 8: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/EditorState.swift Sources/SnapScreenKit/Editor/CanvasView.swift
git commit -m "feat: 펜 도구 등록 + 캔버스 자유곡선 입력"
```

---

### Task 6: 문서 + v0.7.0 범프 + PR

**Files:**
- Modify: `docs/manual-test-checklist.md`, `README.md`, `CLAUDE.md`
- Modify: `Sources/SnapScreenKit/Support/AppInfo.swift`, `Resources/Info.plist`

- [ ] **Step 1: 체크리스트에 "15. 펜" 추가**

`docs/manual-test-checklist.md` 끝에:

```markdown
## 15. 펜 (자유곡선)

- [ ] 펜 도구(또는 P 키)를 선택하고 드래그하면 획이 그려진다
- [ ] 빠르게 그려도 곡선이 부드럽다(각지지 않음)
- [ ] 색 팔레트와 굵기가 획에 반영된다
- [ ] 그린 획을 클릭해 선택하고 드래그로 이동할 수 있다
- [ ] 선택한 획을 ⌫로, 또는 ⌘Z로 삭제/되돌릴 수 있다
- [ ] 클릭만 하고 그리지 않으면 빈 획이 생기지 않는다
- [ ] ⌘S/⌘C로 내보내면 그린 획이 반영된다
```

- [ ] **Step 2: 버전 범프**

- `Sources/SnapScreenKit/Support/AppInfo.swift`: `version = "0.7.0"`
- Info.plist:
```bash
/usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 0.7.0" Resources/Info.plist
/usr/libexec/PlistBuddy -c "Set :CFBundleVersion 9" Resources/Info.plist
```

- [ ] **Step 3: README/CLAUDE.md 갱신**

- `README.md`: Read 후 기능/도구 목록에 "펜(자유곡선)" 추가
- `CLAUDE.md`: Read 후 Editor 모듈 설명에 한 줄 — 펜은 `AnnotationKind.path`로 raw 점열 저장, `PathSmoother`가 렌더 시 이차 베지어 중점 스무딩, 입력은 `CanvasView.penPoints`로 누적.

- [ ] **Step 4: 최종 검증**

```bash
swift test                                            # 전체 PASS (기존 48 + PathSmoother 4 + Pen 5 = 57)
rm -rf .build && swift build 2>&1 | grep -ci warning  # 0
Scripts/bundle.sh release                             # OK
file -I README.md docs/manual-test-checklist.md CLAUDE.md   # utf-8
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" Resources/Info.plist  # 0.7.0
/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" Resources/Info.plist              # 9
```

- [ ] **Step 5: Commit + Push + PR**

```bash
git add docs/ README.md CLAUDE.md Sources/ Resources/
git commit -m "chore: 펜 도구 문서 + v0.7.0 버전 범프"
git push -u origin feat/pen-tool
gh pr create --title "feat: 펜(자유곡선) 도구" --body "$(cat <<'EOF'
## Summary
- 편집기에 손으로 그리는 자유곡선 펜 도구 추가 (단축키 P, 기존 색/굵기 팔레트 재사용)
- 저장은 raw 점열(`AnnotationKind.path`), 렌더 시에만 `PathSmoother`(이차 베지어 중점 스무딩)로 곡선화
- 그린 획은 다른 주석과 동일하게 클릭 선택 / 드래그 이동 / undo·⌫ 삭제, ⌘S/⌘C 반영
- 히트 테스트는 획 선분 근접 판정(화살표 선분 히트 확장)
- v0.7.0 범프

## 설계/계획
- Spec: `docs/superpowers/specs/2026-07-07-pen-tool-design.md`
- Plan: `docs/superpowers/plans/2026-07-07-pen-tool.md`

## Test plan
- [x] 단위: PathSmoother(4), Pen 모델·히트(5), 전체 clean 빌드 경고 0
- [ ] 수동: 그리기·부드러움·색/굵기·선택 이동·삭제·저장 반영 (checklist §15)
EOF
)"
```

커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 추가. `--reviewer` 플래그 쓰지 말 것.

## 실행 순서와 검증 한계

- Task 1(스무딩) → 2(모델) → 3(렌더) → 4(히트) → 5(도구+입력) → 6(문서·범프·PR). Task 2~4는 switch exhaustiveness 때문에 전체 컴파일이 함께 통과해야 하므로, 구현 시 Task 2 케이스 추가 직후 Task 3·4의 케이스도 넣어 빌드를 맞춘 뒤 각 테스트를 돌린다(커밋은 태스크 단위 유지).
- 실제 그리기·부드러움·이동은 GUI라 자동 테스트 불가 → 수동 §15. 단위 테스트는 스무딩 경로 생성·모델·히트 로직만 커버.
- 릴리스(`make release VERSION=v0.7.0`)는 PR 머지 후 별도.
