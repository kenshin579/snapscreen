# 블러 도구 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 가우시안 블러 주석 도구(G) 추가 + 기존 픽셀레이트 도구 라벨을 "모자이크"로 정정 (v0.3.0)

**Architecture:** 기존 주석 아키텍처에 `.blur(CGRect)` 케이스를 얹는다 — 모델(AnnotationKind)/히트 테스트/렌더러(CIGaussianBlur + clampedToExtent 가장자리 처리, 기존 캐시 공유)/도구(EditorTool)/캔버스 드래그 분기. 파이프라인은 pixelate와 평행 구조.

**Tech Stack:** Swift, CoreImage(CIGaussianBlur), 기존 SnapScreenKit 주석 시스템

**전제:**
- 브랜치 `feature/blur-tool`에서 작업 (설계 문서 커밋 있음). main 커밋 금지
- 커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 컨벤션: 모델 계층 AppKit 비의존, UI/렌더러 @MainActor, 한글 파일 `file -I` UTF-8 확인

---

### Task 1: 모델 + 히트 테스트 (.blur 케이스, TDD)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/Annotation.swift`
- Modify: `Sources/SnapScreenKit/Editor/AnnotationHitTester.swift`
- Test: `Tests/SnapScreenKitTests/AnnotationStoreTests.swift` (translate 테스트 1개 추가)
- Test: `Tests/SnapScreenKitTests/AnnotationHitTesterTests.swift` (히트 테스트 1개 추가)

- [ ] **Step 1: 실패하는 테스트 작성**

`AnnotationStoreTests.swift`에 추가:

```swift
    func testTranslateBlur() {
        let store = AnnotationStore()
        let a = Annotation(kind: .blur(CGRect(x: 10, y: 20, width: 100, height: 50)))
        store.add(a)
        store.translate(id: a.id, by: CGVector(dx: 5, dy: -5))
        guard case .blur(let r) = store.annotations[0].kind else {
            return XCTFail("kind changed")
        }
        XCTAssertEqual(r, CGRect(x: 15, y: 15, width: 100, height: 50))
    }
```

`AnnotationHitTesterTests.swift`에 추가:

```swift
    func testBlurHitsInside() {
        let a = Annotation(kind: .blur(CGRect(x: 0, y: 0, width: 100, height: 100)))
        XCTAssertNotNil(AnnotationHitTester.hitTest(CGPoint(x: 50, y: 50), annotations: [a]))
        XCTAssertNil(AnnotationHitTester.hitTest(CGPoint(x: 200, y: 200), annotations: [a]))
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter testTranslateBlur`
Expected: 컴파일 에러 — `type 'AnnotationKind' has no member 'blur'`

- [ ] **Step 3: 모델 구현** — `Annotation.swift`

`AnnotationKind`에 케이스 추가 (`case pixelate(CGRect)` 다음 줄):

```swift
    case blur(CGRect)
```

`translated(by:)`의 switch에 추가 (`case .pixelate` 아래):

```swift
        case .blur(let r): return .blur(r.offsetBy(dx: d.dx, dy: d.dy))
```

`bounds`의 rect 묶음 케이스에 합류 — 기존 줄을 다음으로 교체:

```swift
        case .rectangle(let r), .ellipse(let r), .pixelate(let r), .blur(let r):
            return r
```

- [ ] **Step 4: 히트 테스터 구현** — `AnnotationHitTester.swift`

기존 `case .text, .pixelate:` 분기를 다음으로 교체:

```swift
        case .text, .pixelate, .blur:
            return a.kind.bounds.insetBy(dx: -tol, dy: -tol).contains(p)
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `swift test`
Expected: 전부 PASS (기존 35 + 신규 2 = 37개)

- [ ] **Step 6: Commit**

```bash
git add Sources/ Tests/
git commit -m "feat: 블러 주석 모델 + 히트 테스트 (.blur 케이스)"
```

---

### Task 2: 렌더러 (CIGaussianBlur) + Flatten 테스트 (TDD)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/AnnotationRenderer.swift`
- Test: `Tests/SnapScreenKitTests/FlattenRendererTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성** — `FlattenRendererTests.swift`에 추가 (기존 `makeImage` 헬퍼 재사용, @MainActor 표기 — 기존 테스트들과 동일):

```swift
    @MainActor
    func testFlattenWithBlurPreservesDimensions() {
        let base = makeImage(width: 200, height: 100)
        let a = Annotation(kind: .blur(CGRect(x: 10, y: 10, width: 80, height: 40)))
        let out = FlattenRenderer.flatten(image: base, annotations: [a], scale: 2)
        XCTAssertEqual(out?.width, 200)
        XCTAssertEqual(out?.height, 100)
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter testFlattenWithBlurPreservesDimensions`
Expected: 컴파일 에러 (Task 1 완료 상태라면 렌더러의 switch가 non-exhaustive라 빌드 실패했을 것 — Task 1에서 이미 케이스 추가로 컴파일이 깨진다면, Task 1의 Step 5 전에 렌더러에 임시 `case .blur: break`를 넣지 말고 **Task 1과 Task 2를 같은 세션에서 연속 진행**하라. 즉 Task 1 Step 5의 `swift test`는 이 Task의 Step 3 구현 후에야 통과한다 — 그 경우 Task 1 커밋과 Task 2 커밋을 합쳐 하나의 커밋으로 만들어도 좋다: `feat: 블러 주석 도구 데이터/렌더 계층`)

- [ ] **Step 3: 렌더러 구현** — `AnnotationRenderer.swift`

`draw(_ annotation:...)`의 switch에 케이스 추가 (`case .pixelate` 블록 다음):

```swift
        case .blur(let rect):
            if let cached = pixelateCache[annotation.id], cached.rect == rect {
                ctx.draw(cached.image, in: cached.clamped)
            } else if let result = blurredImage(from: baseImage, rect: rect, scale: scale) {
                if pixelateCache.count >= 64 { pixelateCache.removeAll() } // 무한 성장 방지
                pixelateCache[annotation.id] = (rect, result.image, result.rect)
                ctx.draw(result.image, in: result.rect)
            }
```

`pixelatedImage(...)` 아래에 메서드 추가:

```swift
    /// 가우시안 블러 — 시각적 완화용. 민감정보 가리기에는 모자이크(pixelate)를 권장
    /// (약한 가우시안 블러는 복원 공격 가능).
    static func blurredImage(from base: CGImage, rect: CGRect,
                             scale: CGFloat) -> (image: CGImage, rect: CGRect)? {
        let clamped = rect.intersection(CGRect(x: 0, y: 0, width: base.width, height: base.height))
        guard !clamped.isEmpty else { return nil }
        // clampedToExtent: 블러가 경계 밖 투명 픽셀을 섞어 가장자리가 어두워지는 것을 방지
        let input = CIImage(cgImage: base).cropped(to: clamped).clampedToExtent()
        guard let filter = CIFilter(name: "CIGaussianBlur") else { return nil }
        filter.setValue(input, forKey: kCIInputImageKey)
        let radius = max(8 * scale, min(clamped.width, clamped.height) / 24)
        filter.setValue(radius, forKey: kCIInputRadiusKey)
        guard let output = filter.outputImage?.cropped(to: clamped) else { return nil }
        guard let image = ciContext.createCGImage(output, from: clamped) else { return nil }
        return (image, clamped)
    }
```

캐시 주석 갱신 — `pixelateCache` 선언 위 주석을 다음으로 교체:

```swift
    /// 픽셀레이트/블러 결과 캐시 — 매 draw마다 CIFilter 재실행을 피한다 (드래그 리렌더 잔크 방지).
    /// baseImage는 편집기 세션 동안 불변이므로 rect 비교만으로 무효화 가능. 키는 annotation UUID라
    /// 두 도구가 공유해도 충돌 없음.
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test`
Expected: 전부 PASS (37 + 1 = 38개)

- [ ] **Step 5: 블러 스모크 검증 (임시 파일, 커밋 금지)**

임시 테스트로 다음을 확인 후 삭제:
- 그라데이션 이미지(96×96)에 blur 적용 → 원본과 픽셀 값이 다름
- 같은 영역에 pixelate 적용한 결과와도 다름 (블록 경계 없음 — 연속 그라데이션 유지)
- radius 검증: scale=2, 100×100 영역 → radius = max(16, 100/24) = 16

삭제 후 `swift test` 38개 재확인 + `git status` 클린.

- [ ] **Step 6: Commit**

```bash
git add Sources/ Tests/
git commit -m "feat: 블러 렌더러 (CIGaussianBlur + 가장자리 clamp + 캐시 공유)"
```

(Task 1과 합친 경우: `feat: 블러 주석 도구 데이터/렌더 계층` 단일 커밋)

---

### Task 3: 도구 노출 (EditorTool + CanvasView) + 라벨 정정

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/EditorState.swift`
- Modify: `Sources/SnapScreenKit/Editor/CanvasView.swift`

- [ ] **Step 1: EditorTool 확장** — `EditorState.swift`

케이스 선언을 다음으로 교체 (blur를 pixelate 앞에 배치 — 툴바에서 블러·모자이크 인접):

```swift
    case arrow, rectangle, ellipse, text, blur, pixelate, stepBadge
```

label switch를 다음으로 교체 (pixelate 라벨 정정 포함):

```swift
    public var label: String {
        switch self {
        case .arrow: return "화살표"
        case .rectangle: return "사각형"
        case .ellipse: return "원"
        case .text: return "텍스트"
        case .blur: return "블러 (시각적 완화용 — 민감정보는 모자이크 사용)"
        case .pixelate: return "모자이크 (민감정보 가리기)"
        case .stepBadge: return "번호"
        }
    }
```

symbolName switch에 추가 (`case .text` 아래):

```swift
        case .blur: return "drop.halffull"
```

- [ ] **Step 2: CanvasView 배선** — `CanvasView.swift`

`keyDown`의 도구 매핑 딕셔너리를 다음으로 교체 (`g` 추가):

```swift
            let mapping: [String: EditorTool] = [
                "a": .arrow, "r": .rectangle, "o": .ellipse,
                "t": .text, "g": .blur, "b": .pixelate, "n": .stepBadge
            ]
```

`makeDraft`의 switch에서 `case .pixelate:` 줄을 다음으로 교체:

```swift
        case .pixelate: kind = .pixelate(rect)
        case .blur: kind = .blur(rect)
```

- [ ] **Step 3: 빌드 + 테스트 + 상주 확인**

Run: `swift test && Scripts/run.sh` → `pgrep -x SnapScreen` → `pkill -x SnapScreen`
Expected: 38개 PASS. 육안 확인 항목 보고: 툴바에 도구 7종(블러가 모자이크 앞), 라벨 툴팁 문구, `G` 키 → 블러 드래그 → 부드럽게 흐려짐(블록 패턴 없음), `B` 키 → 모자이크(기존 동작)

- [ ] **Step 4: Commit**

```bash
git add Sources/
git commit -m "feat: 블러 도구 노출 (G) + 모자이크 라벨 정정"
```

---

### Task 4: 문서 + v0.3.0 범프 + PR

**Files:**
- Modify: `docs/manual-test-checklist.md` (§5에 2항목 추가)
- Modify: `README.md` (도구 목록에 블러 추가 + 사용 구분 1줄)
- Modify: `CLAUDE.md` (도구 단축키 언급이 있으면 갱신 — grep으로 확인)
- Modify: `Sources/SnapScreenKit/Support/AppInfo.swift` (0.2.0 → 0.3.0)
- Modify: `Resources/Info.plist` (0.3.0, CFBundleVersion 3)

- [ ] **Step 1: 체크리스트 §5에 추가** (텍스트 입력 항목들 아래):

```markdown
- [ ] 블러 도구(G)로 가린 영역이 부드럽게 흐려진다 (모자이크와 달리 블록 패턴이 없음)
- [ ] 모자이크 도구 라벨/툴팁이 "모자이크"로 표시된다
```

- [ ] **Step 2: README 갱신** — 기능 목록의 주석 도구 나열에 "블러" 추가, 아래 안내 1줄 추가:

```markdown
> 민감정보 가리기에는 **모자이크**를 사용하세요 — 약한 블러는 복원될 수 있습니다.
```

- [ ] **Step 3: 버전 범프**

- `AppInfo.swift`: `version = "0.3.0"`
- `Resources/Info.plist`: `CFBundleShortVersionString` → `0.3.0`, `CFBundleVersion` → `3`

- [ ] **Step 4: 최종 검증**

```bash
swift test                        # 38개 PASS
Scripts/bundle.sh release         # OK
file -I README.md docs/manual-test-checklist.md   # utf-8
grep -rn "A/R/O/T/B/N" README.md CLAUDE.md        # 단축키 표기 갱신 필요 여부 확인 (G 추가)
```

- [ ] **Step 5: Commit + PR**

```bash
git add docs/ README.md CLAUDE.md Sources/ Resources/
git commit -m "chore: 블러 도구 문서 + v0.3.0 버전 범프"
git push -u origin feature/blur-tool
gh pr create --title "feat: 블러 도구 (가우시안) + 모자이크 라벨 정정" --body "$(cat <<'EOF'
## Summary
- 가우시안 블러 주석 도구 추가 (단축키 G) — 시각적 완화용, CIGaussianBlur + clampedToExtent 가장자리 처리
- 기존 픽셀레이트 도구 라벨 "블러" → "모자이크" 정정 (동작 변경 없음)
- 툴팁/README에 사용 구분 명시: 민감정보는 모자이크 (블러는 복원 가능)
- 렌더 캐시(pixelateCache)를 두 도구가 공유, v0.3.0 버전 범프

## 설계/계획
- Spec: `docs/superpowers/specs/2026-07-05-blur-tool-design.md`
- Plan: `docs/superpowers/plans/2026-07-05-blur-tool.md`

## Test plan
- [ ] `swift test` 38개 통과 (blur translate/hit/flatten 신규 3개 포함)
- [ ] 수동: G 드래그 → 부드러운 블러(블록 없음), B → 모자이크, 라벨 확인 (checklist §5)
EOF
)"
```

---

## 실행 순서

Task 1 → 2는 렌더러 switch exhaustiveness 때문에 사실상 연속 작업 (같은 구현자가 연속 수행, 커밋 합치기 허용). Task 3 → 4 순차. 릴리스(`make release VERSION=v0.3.0`)는 PR 머지 후 별도.
