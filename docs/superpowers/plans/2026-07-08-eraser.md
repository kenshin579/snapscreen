# 지우개 도구 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 편집기에 드래그로 문질러 지우는 지우개 도구를 추가한다(펜 획은 부분 분할, 그 외 주석은 통째 삭제, undo 1회, 원형 커서).

**Architecture:** `PathEraser`(순수 함수)가 점열에서 지우개 반경 안 점을 제거해 조각으로 분할한다. `CanvasView`가 드래그 동안 로컬 미리보기 배열로 렌더하고, 놓을 때 `AnnotationStore.replace(with:)`로 전체를 1회 커밋(undo 1회). 원형 커서는 `NSTrackingArea`+`mouseMoved`로 표시.

**Tech Stack:** Swift, CoreGraphics, AppKit, XCTest.

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `Editor/PathEraser.swift` | 점열+중심들+반경 → 조각들 순수 함수 | 신규 |
| `Editor/AnnotationStore.swift` | `replace(with:)` 배치 트랜잭션 | 수정 |
| `Editor/EditorState.swift` | `EditorTool.eraser` 등록 | 수정 |
| `Editor/CanvasView.swift` | 지우개 입력·미리보기·커서·단축키·draw 분기 | 수정 |
| `Editor/EditorWindowController.swift` | 도구 전환 시 지우개 상태 정리(sink) | 수정 |
| `Tests/SnapScreenKitTests/PathEraserTests.swift` | 순수 함수 테스트 | 신규 |
| `Tests/SnapScreenKitTests/AnnotationStoreReplaceTests.swift` | replace undo 테스트 | 신규 |
| `docs/manual-test-checklist.md`, `README.md`, `CLAUDE.md`, `Support/AppInfo.swift`, `Resources/Info.plist` | 문서·버전 | 수정 |

---

### Task 1: PathEraser (순수 함수, TDD)

**Files:**
- Create: `Sources/SnapScreenKit/Editor/PathEraser.swift`
- Test: `Tests/SnapScreenKitTests/PathEraserTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`Tests/SnapScreenKitTests/PathEraserTests.swift`:

```swift
import XCTest
import CoreGraphics
@testable import SnapScreenKit

final class PathEraserTests: XCTestCase {
    // 가로 일직선 점열 (y=0), x=0,10,20,...,100
    private let line = (0...10).map { CGPoint(x: CGFloat($0 * 10), y: 0) }

    func testNothingErasedReturnsOriginal() {
        let out = PathEraser.erase(line, along: [CGPoint(x: 50, y: 100)], radius: 5)
        XCTAssertEqual(out.count, 1)
        XCTAssertEqual(out[0], line)
    }

    func testMiddleEraseSplitsIntoTwo() {
        // x=50 중심 반경 5 → x=50 점만 제거 → [0..40], [60..100]
        let out = PathEraser.erase(line, along: [CGPoint(x: 50, y: 0)], radius: 5)
        XCTAssertEqual(out.count, 2)
        XCTAssertEqual(out[0].first, CGPoint(x: 0, y: 0))
        XCTAssertEqual(out[0].last, CGPoint(x: 40, y: 0))
        XCTAssertEqual(out[1].first, CGPoint(x: 60, y: 0))
        XCTAssertEqual(out[1].last, CGPoint(x: 100, y: 0))
    }

    func testEndEraseKeepsOneSegment() {
        // x=100 끝점만 제거 → [0..90] 한 조각
        let out = PathEraser.erase(line, along: [CGPoint(x: 100, y: 0)], radius: 5)
        XCTAssertEqual(out.count, 1)
        XCTAssertEqual(out[0].last, CGPoint(x: 90, y: 0))
    }

    func testEraseAllReturnsEmpty() {
        // 전체를 덮는 큰 반경
        let out = PathEraser.erase(line, along: [CGPoint(x: 50, y: 0)], radius: 1000)
        XCTAssertEqual(out.count, 0)
    }

    func testSinglePointSegmentsDropped() {
        // 3점에서 가운데(x=30)만 제거하면 양옆이 각각 1점 → 둘 다 버려져 빈 배열
        let pts = [CGPoint(x: 0, y: 0), CGPoint(x: 30, y: 0), CGPoint(x: 60, y: 0)]
        let out = PathEraser.erase(pts, along: [CGPoint(x: 30, y: 0)], radius: 5)
        XCTAssertEqual(out.count, 0)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter PathEraserTests`
Expected: 컴파일 실패 (`PathEraser` 없음).

- [ ] **Step 3: PathEraser 구현**

`Sources/SnapScreenKit/Editor/PathEraser.swift`:

```swift
import CoreGraphics

/// 자유곡선 점열에서 지우개 경로(centers) 반경 안의 점을 제거하고,
/// 남은 인덱스-연속 구간을 조각으로 분리하는 순수 함수. AppKit 비의존.
public enum PathEraser {
    public static func erase(_ points: [CGPoint],
                             along centers: [CGPoint],
                             radius: CGFloat) -> [[CGPoint]] {
        guard !points.isEmpty else { return [] }
        func isErased(_ p: CGPoint) -> Bool {
            centers.contains { hypot(p.x - $0.x, p.y - $0.y) <= radius }
        }
        var segments: [[CGPoint]] = []
        var current: [CGPoint] = []
        var erasedAny = false
        for p in points {
            if isErased(p) {
                erasedAny = true
                if current.count >= 2 { segments.append(current) }
                current = []
            } else {
                current.append(p)
            }
        }
        if current.count >= 2 { segments.append(current) }
        // 아무것도 안 지워졌으면 원본 그대로 반환(호출부의 "변경 없음" 판정을 위해)
        if !erasedAny { return [points] }
        return segments
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test --filter PathEraserTests`
Expected: 5 tests PASS.

- [ ] **Step 5: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/PathEraser.swift Tests/SnapScreenKitTests/PathEraserTests.swift
git commit -m "feat: 자유곡선 부분 지우기 순수 함수 (PathEraser)"
```

---

### Task 2: AnnotationStore.replace (배치 트랜잭션, TDD)

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/AnnotationStore.swift`
- Test: `Tests/SnapScreenKitTests/AnnotationStoreReplaceTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`Tests/SnapScreenKitTests/AnnotationStoreReplaceTests.swift`:

```swift
import XCTest
import CoreGraphics
@testable import SnapScreenKit

final class AnnotationStoreReplaceTests: XCTestCase {
    func testReplaceThenUndoRestoresPrevious() {
        let store = AnnotationStore()
        let a = Annotation(kind: .rectangle(CGRect(x: 0, y: 0, width: 10, height: 10)))
        store.add(a)
        XCTAssertEqual(store.annotations.count, 1)

        let b = Annotation(kind: .ellipse(CGRect(x: 5, y: 5, width: 20, height: 20)))
        store.replace(with: [b])
        XCTAssertEqual(store.annotations.count, 1)
        XCTAssertEqual(store.annotations[0].id, b.id)

        store.undo()   // replace 1회만 되돌림
        XCTAssertEqual(store.annotations.count, 1)
        XCTAssertEqual(store.annotations[0].id, a.id)

        store.redo()
        XCTAssertEqual(store.annotations[0].id, b.id)
    }

    func testReplaceWithEmptyClearsAndUndoRestores() {
        let store = AnnotationStore()
        store.add(Annotation(kind: .rectangle(CGRect(x: 0, y: 0, width: 1, height: 1))))
        store.replace(with: [])
        XCTAssertTrue(store.annotations.isEmpty)
        store.undo()
        XCTAssertEqual(store.annotations.count, 1)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter AnnotationStoreReplaceTests`
Expected: 컴파일 실패 (`replace(with:)` 없음).

- [ ] **Step 3: replace 구현**

`AnnotationStore`에 메서드 추가(`translate` 다음):

```swift
    /// 여러 주석을 한 번에 교체 (지우개 등 배치 변경). undo 1회로 복원된다.
    public func replace(with newAnnotations: [Annotation]) {
        snapshot()
        annotations = newAnnotations
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test --filter AnnotationStoreReplaceTests`
Expected: 2 tests PASS.

- [ ] **Step 5: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/AnnotationStore.swift Tests/SnapScreenKitTests/AnnotationStoreReplaceTests.swift
git commit -m "feat: AnnotationStore.replace 배치 트랜잭션 (undo 1회)"
```

---

### Task 3: 도구 등록 + 캔버스 지우개 입력/미리보기/커서

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/EditorState.swift`
- Modify: `Sources/SnapScreenKit/Editor/CanvasView.swift`
- Modify: `Sources/SnapScreenKit/Editor/EditorWindowController.swift`

- [ ] **Step 1: EditorTool.eraser 등록**

`EditorState.swift`:
- `case arrow, ... , pen` 끝에 `, eraser` 추가
- `label` switch: `case .eraser: return "지우개"`
- `symbolName` switch: `case .eraser: return "eraser"`

- [ ] **Step 2: CanvasView 상태 프로퍼티 + 헬퍼 추가**

`CanvasView`에 프로퍼티 추가(`penPoints` 근처):

```swift
    // 지우개: 드래그 중 지우개 중심 누적(이미지 픽셀). nil이면 지우개 드래그 중 아님.
    private var eraseCenters: [CGPoint]?
    private var erasePreview: [Annotation] = []
    // 지우개 원형 커서 위치(이미지 픽셀). nil이면 미표시.
    private var eraserCursor: CGPoint?
    /// 뷰 기준 지름 24pt → 이미지 픽셀 반경
    private var eraserRadiusInImage: CGFloat { 12 / fitScale }
```

헬퍼 메서드 추가(클래스 내):

```swift
    /// 현재 store.annotations에 지우개 중심들을 적용한 결과 배열을 만든다.
    /// 펜 획은 조각으로 분할, 그 외 주석은 반경 히트 시 통째 제거. 변경 없는 획은 원본(UUID) 유지.
    private func erasedAnnotations(centers: [CGPoint]) -> [Annotation] {
        let r = eraserRadiusInImage
        var result: [Annotation] = []
        for a in store.annotations {
            if case .path(let pts) = a.kind {
                let segments = PathEraser.erase(pts, along: centers, radius: r)
                if segments.count == 1, segments[0] == pts {
                    result.append(a) // 변경 없음 → 원본 유지(UUID 보존)
                } else {
                    for seg in segments {
                        result.append(Annotation(kind: .path(seg), color: a.color, lineWidth: a.lineWidth))
                    }
                }
            } else {
                let hit = centers.contains {
                    AnnotationHitTester.hitTest($0, annotations: [a], tolerance: r) != nil
                }
                if !hit { result.append(a) }
            }
        }
        return result
    }

    /// 도구 전환 등으로 진행 중 지우개를 취소(미리보기 폐기, store 미변경).
    func cancelEraseIfActive() {
        if eraseCenters != nil {
            eraseCenters = nil
            erasePreview = []
            needsDisplay = true
        }
    }
```

- [ ] **Step 3: mouseDown/Dragged/Up에 지우개 분기**

`mouseDown`에서 crop 분기(`if isCropping { ... return }`) **다음**, 이미지 영역 가드 **앞**에 추가:

```swift
        if state.tool == .eraser {
            eraseCenters = [p]
            erasePreview = erasedAnnotations(centers: [p])
            needsDisplay = true
            return
        }
```

`mouseDragged`에서 펜 분기(`if penPoints != nil { ... return }`) **다음**에 추가:

```swift
        if eraseCenters != nil {
            let p = imagePoint(from: event)
            eraseCenters?.append(p)
            erasePreview = erasedAnnotations(centers: eraseCenters!)
            needsDisplay = true
            return
        }
```

`mouseUp`에서 펜 분기(`if let pts = penPoints { ... return }`) **다음**에 추가:

```swift
        if eraseCenters != nil {
            if erasePreview != store.annotations {
                store.replace(with: erasePreview)
            }
            eraseCenters = nil
            erasePreview = []
            needsDisplay = true
            return
        }
```

- [ ] **Step 4: mouseMoved + updateTrackingAreas (원형 커서)**

`CanvasView`에 오버라이드 추가:

```swift
    public override func updateTrackingAreas() {
        super.updateTrackingAreas()
        for area in trackingAreas { removeTrackingArea(area) }
        addTrackingArea(NSTrackingArea(
            rect: bounds,
            options: [.mouseMoved, .activeInKeyWindow, .inVisibleRect],
            owner: self, userInfo: nil))
    }

    public override func mouseMoved(with event: NSEvent) {
        guard state.tool == .eraser else {
            if eraserCursor != nil { eraserCursor = nil; needsDisplay = true }
            return
        }
        eraserCursor = imagePoint(from: event)
        needsDisplay = true
    }
```

- [ ] **Step 5: draw 분기 (미리보기 렌더 + 커서 원)**

`draw(_:)`의 `for annotation in store.annotations {` 를 아래로 교체(지우개 중이면 미리보기 배열 렌더):

```swift
        let toRender = (eraseCenters != nil) ? erasePreview : store.annotations
        for annotation in toRender {
```

`drawOverlays(in:)` 끝(또는 `draw`의 `drawOverlays(in: ctx)` 호출 직후, `ctx.restoreGState()` 앞)에 지우개 커서 원 추가. `drawOverlays`가 이미 scaled ctx(이미지 좌표) 안에서 호출되므로 그 안에 추가:

```swift
        if state.tool == .eraser, let c = eraserCursor {
            let r = eraserRadiusInImage
            ctx.setStrokeColor(NSColor.gray.withAlphaComponent(0.85).cgColor)
            ctx.setLineWidth(1 / fitScale)
            ctx.strokeEllipse(in: CGRect(x: c.x - r, y: c.y - r, width: r * 2, height: r * 2))
        }
```

- [ ] **Step 6: 단축키 x**

`keyDown`의 `mapping` 딕셔너리에 `"x": .eraser` 추가:

```swift
            let mapping: [String: EditorTool] = [
                "a": .arrow, "r": .rectangle, "o": .ellipse,
                "t": .text, "g": .blur, "b": .pixelate, "n": .stepBadge,
                "p": .pen, "x": .eraser
            ]
```

`makeDraft(from:to:)`의 `switch state.tool`에 exhaustive 처리가 필요하면 `.eraser`를 `.text, .stepBadge, .pen`과 함께 넣어라(`case .text, .stepBadge, .pen, .eraser: kind = .rectangle(rect)` — 지우개는 makeDraft에 도달하지 않지만 exhaustiveness용). 빌드해서 확인 후 조치.

- [ ] **Step 7: EditorWindowController — 도구 전환 시 지우개 정리**

`toolCancellable = state.$tool.sink { ... }` 클로저에 지우개 정리 추가:

```swift
        toolCancellable = state.$tool.sink { [weak self] _ in
            self?.canvas.cancelCropIfActive()
            self?.canvas.cancelEraseIfActive()
            self?.canvas.needsDisplay = true  // 커서 표시/숨김 갱신
        }
```

- [ ] **Step 8: 빌드 + 로컬 실행 확인**

Run: `rm -rf .build && swift build 2>&1 | grep -ci warning` → 0
Run: `swift test 2>&1 | grep -E "Executed [0-9]+ tests" | tail -1` → 이전 대비 +7 (PathEraser 5 + replace 2)
Run: `Scripts/run.sh` → 펜으로 긋고 지우개(X)로 문질러 분할/삭제 확인(수동)

- [ ] **Step 9: 커밋**

```bash
git add Sources/SnapScreenKit/Editor/EditorState.swift Sources/SnapScreenKit/Editor/CanvasView.swift Sources/SnapScreenKit/Editor/EditorWindowController.swift
git commit -m "feat: 지우개 도구 (펜 부분 지우기 + 통째 삭제 + 원형 커서)"
```

---

### Task 4: 문서 + v0.9.0 범프 + PR

**Files:**
- Modify: `docs/manual-test-checklist.md`, `README.md`, `CLAUDE.md`
- Modify: `Sources/SnapScreenKit/Support/AppInfo.swift`, `Resources/Info.plist`

- [ ] **Step 1: 체크리스트 "18. 지우개" 추가**

`docs/manual-test-checklist.md` 끝에:

```markdown
## 18. 지우개

- [ ] 지우개(또는 X 키) 선택 시 마우스를 따라 원형 커서가 표시된다
- [ ] 펜 획 가운데를 문지르면 획이 두 조각으로 나뉜다
- [ ] 펜 획 끝을 문지르면 그 부분만 짧아진다
- [ ] 사각형·화살표·텍스트·번호 배지는 지우개가 닿으면 통째로 삭제된다
- [ ] 한 번 드래그로 여러 개를 지운 뒤 ⌘Z 한 번이면 전부 복원된다
- [ ] 지운 결과가 ⌘S/⌘C에 반영된다
- [ ] 창을 확대/축소해도 커서 원 크기와 실제 지워지는 범위가 일치한다
```

- [ ] **Step 2: 버전 범프**

- `Sources/SnapScreenKit/Support/AppInfo.swift`: `version = "0.9.0"`
- Info.plist:
```bash
/usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 0.9.0" Resources/Info.plist
/usr/libexec/PlistBuddy -c "Set :CFBundleVersion 13" Resources/Info.plist
```

- [ ] **Step 3: README/CLAUDE.md 갱신**

- `README.md`: 도구 목록에 "지우개(자유곡선 부분 지우기·주석 삭제)" 추가, 단축키 표에 `| 지우개 | X |` 행 추가
- `CLAUDE.md`: Editor 모듈 설명에 한 줄 — 지우개는 `PathEraser`(순수 함수)로 펜 획을 조각 분할하고 그 외 주석은 통째 삭제하며, 드래그 결과를 `AnnotationStore.replace`로 undo 1회 커밋, 원형 커서는 trackingArea/mouseMoved로 표시한다는 취지.

- [ ] **Step 4: 최종 검증**

```bash
swift test 2>&1 | grep -E "Executed [0-9]+ tests" | tail -1        # 이전 62 + 7 = 69 PASS
rm -rf .build && swift build 2>&1 | grep -ci warning               # 0
Scripts/bundle.sh release                                          # OK
file -I README.md docs/manual-test-checklist.md CLAUDE.md          # utf-8
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" Resources/Info.plist  # 0.9.0
/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" Resources/Info.plist              # 13
```

- [ ] **Step 5: Commit + Push + PR**

```bash
git add docs/ README.md CLAUDE.md Sources/ Resources/
git commit -m "chore: 지우개 문서 + v0.9.0 버전 범프"
git push -u origin feat/eraser
gh pr create --title "feat: 지우개 도구" --body "$(cat <<'EOF'
## Summary
- 드래그로 문질러 지우는 지우개 도구 (단축키 X, 원형 커서)
- 펜 획(path)은 반경 안 점을 제거해 조각으로 분할, 그 외 주석은 닿으면 통째 삭제
- 드래그 결과를 `AnnotationStore.replace`로 undo 1회 커밋
- `PathEraser` 순수 함수(점 제거·조각 분할) + 단위 테스트
- v0.9.0 범프

## 설계/계획
- Spec: `docs/superpowers/specs/2026-07-08-eraser-design.md`
- Plan: `docs/superpowers/plans/2026-07-08-eraser.md`

## Test plan
- [x] 단위: PathEraser(5), AnnotationStore.replace(2), 전체 69개 통과, clean 빌드 경고 0
- [ ] 수동: 펜 분할·통째 삭제·원형 커서·undo 1회·저장 반영·확대축소 반경 (checklist §18)
EOF
)"
```

커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 추가. `--reviewer` 플래그 쓰지 말 것.

## 실행 순서와 검증 한계

- Task 1(PathEraser) → 2(replace) → 3(도구+캔버스 통합) → 4(문서·범프·PR).
- 실제 문지르기·분할·커서·확대축소 반경은 GUI라 자동 테스트 불가 → 수동 §18. 단위 테스트는 `PathEraser` 분할 로직과 `replace` undo만 커버.
- 릴리스(`make release VERSION=v0.9.0`)는 PR 머지 후 별도.
