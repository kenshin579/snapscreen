# 앱 아이콘 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SnapScreen에 전용 앱 아이콘(캡처 스택/바이올렛)을 제작해 `.icns`로 빌드하고 앱 번들에 통합한다.

**Architecture:** SVG 소스(`Resources/AppIcon.svg`)를 `rsvg-convert`로 사이즈별 PNG 래스터화 → `.iconset` → `iconutil`로 `Resources/AppIcon.icns` 생성(`Scripts/make-icon.sh`). `Info.plist`의 `CFBundleIconFile`과 `bundle.sh`의 리소스 복사로 번들에 반영. SVG·icns 모두 커밋해 빌드 시 렌더 도구 의존을 없앤다.

**Tech Stack:** SVG, `rsvg-convert`/`cairosvg`(폴백), `sips`, `iconutil`, bash.

---

## 파일 구조

| 파일 | 책임 | 상태 |
|---|---|---|
| `Resources/AppIcon.svg` | 아이콘 벡터 소스(512 viewBox) | 신규 |
| `Scripts/make-icon.sh` | SVG → iconset → `.icns` 생성 (재실행 가능) | 신규 |
| `Resources/AppIcon.icns` | 빌드 산출물(커밋 대상) | 신규(생성) |
| `Resources/Info.plist` | `CFBundleIconFile` 키 추가 + 버전 범프 | 수정 |
| `Scripts/bundle.sh` | icns를 `Contents/Resources/`로 복사 | 수정 |
| `Sources/SnapScreenKit/Support/AppInfo.swift` | 버전 0.6.0 | 수정 |
| `docs/manual-test-checklist.md`, `README.md`, `CLAUDE.md` | 문서 | 수정 |

---

### Task 1: 아이콘 SVG 소스 + 렌더 검증

**Files:**
- Create: `Resources/AppIcon.svg`

- [ ] **Step 1: SVG 소스 작성**

`Resources/AppIcon.svg` 생성 (설계 문서 §2 확정 소스):

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#6a5cff"/><stop offset="1" stop-color="#a638ff"/>
    </linearGradient>
    <linearGradient id="gloss" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#fff" stop-opacity="0.28"/>
      <stop offset="0.5" stop-color="#fff" stop-opacity="0"/>
    </linearGradient>
    <filter id="card" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="0" dy="10" stdDeviation="14" flood-color="#2a0a4a" flood-opacity="0.35"/>
    </filter>
    <filter id="soft" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="0" dy="6" stdDeviation="8" flood-color="#2a0a4a" flood-opacity="0.28"/>
    </filter>
  </defs>
  <rect width="512" height="512" rx="114" fill="url(#bg)"/>
  <rect width="512" height="512" rx="114" fill="url(#gloss)"/>
  <g transform="rotate(-12 256 256)">
    <rect x="150" y="150" width="212" height="212" rx="30" fill="#fff" opacity="0.3" filter="url(#soft)"/>
    <rect x="176" y="176" width="212" height="212" rx="30" fill="#fff" opacity="0.55" filter="url(#soft)"/>
    <rect x="128" y="128" width="212" height="212" rx="30" fill="#fff" filter="url(#card)"/>
    <g fill="none" stroke="#7a4cff" stroke-width="18" stroke-linecap="round" stroke-linejoin="round">
      <path d="M162 198 V178 A8 8 0 0 1 170 170 H190"/>
      <path d="M278 170 H298 A8 8 0 0 1 306 178 V198"/>
      <path d="M306 270 V290 A8 8 0 0 1 298 298 H278"/>
      <path d="M190 298 H170 A8 8 0 0 1 162 290 V270"/>
    </g>
  </g>
</svg>
```

- [ ] **Step 2: rsvg-convert 렌더 + 그림자 필터 확인**

임시 렌더로 `rsvg-convert`가 `feDropShadow`를 처리하는지 확인:

```bash
mkdir -p /tmp/icon-check
rsvg-convert -w 1024 -h 1024 Resources/AppIcon.svg -o /tmp/icon-check/1024.png
sips -g pixelWidth -g pixelHeight /tmp/icon-check/1024.png
```
Expected: `pixelWidth: 1024`, `pixelHeight: 1024`, 오류 없이 PNG 생성.

`open /tmp/icon-check/1024.png`로 육안 확인: 카드 그림자(깊이감)가 렌더되는지. **그림자가 안 보이면** Step 3의 `RENDERER`를 `cairosvg`로 정한다(그 경우 `cairosvg Resources/AppIcon.svg -o /tmp/icon-check/1024c.png --output-width 1024 --output-height 1024`로 재확인). 그림자가 보이면 `rsvg-convert`를 사용한다. 어느 쪽을 쓸지 결정해 Task 2에 반영한다.

- [ ] **Step 3: 작은 크기(16/32px) 가독성 확인**

```bash
rsvg-convert -w 16 -h 16 Resources/AppIcon.svg -o /tmp/icon-check/16.png
rsvg-convert -w 32 -h 32 Resources/AppIcon.svg -o /tmp/icon-check/32.png
open /tmp/icon-check/16.png /tmp/icon-check/32.png
```
Expected: 16/32px에서 스택 형태가 식별 가능. 프레임 코너 브래킷이 완전히 뭉개져 검은 덩어리로 보이면 → 이 계획에서는 **단일 SVG로 진행하되**, 뭉개짐이 심하면 후속 개선 항목으로 기록(README/이슈). MVP는 단일 소스로 확정(YAGNI: variant는 실제 문제 확인 시에만).

- [ ] **Step 4: 커밋**

```bash
git add Resources/AppIcon.svg
git commit -m "feat: 앱 아이콘 SVG 소스 (캡처 스택)"
```

---

### Task 2: `.icns` 생성 스크립트

**Files:**
- Create: `Scripts/make-icon.sh`
- Create(생성): `Resources/AppIcon.icns`

- [ ] **Step 1: make-icon.sh 작성**

`Scripts/make-icon.sh` 생성. (Task 1 Step 2 결정에 따라 `RENDERER` 기본값이 `rsvg-convert`이면 그대로, `cairosvg`면 아래 `render()` 함수의 주석대로 분기.)

```bash
#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")/.."

SVG="Resources/AppIcon.svg"
ICNS="Resources/AppIcon.icns"
ICONSET="build/AppIcon.iconset"

if [ ! -f "$SVG" ]; then
    echo "ERROR: $SVG 없음" >&2; exit 1
fi

# 렌더러: rsvg-convert 우선, 없으면 cairosvg 폴백.
render() {  # render <size> <out>
    local size="$1" out="$2"
    if command -v rsvg-convert >/dev/null 2>&1; then
        rsvg-convert -w "$size" -h "$size" "$SVG" -o "$out"
    elif command -v cairosvg >/dev/null 2>&1; then
        cairosvg "$SVG" -o "$out" --output-width "$size" --output-height "$size"
    else
        echo "ERROR: rsvg-convert 또는 cairosvg 필요" >&2; exit 1
    fi
}

rm -rf "$ICONSET"
mkdir -p "$ICONSET"

# Apple iconset 규격: 파일명 <- 픽셀크기
render 16   "$ICONSET/icon_16x16.png"
render 32   "$ICONSET/icon_16x16@2x.png"
render 32   "$ICONSET/icon_32x32.png"
render 64   "$ICONSET/icon_32x32@2x.png"
render 128  "$ICONSET/icon_128x128.png"
render 256  "$ICONSET/icon_128x128@2x.png"
render 256  "$ICONSET/icon_256x256.png"
render 512  "$ICONSET/icon_256x256@2x.png"
render 512  "$ICONSET/icon_512x512.png"
render 1024 "$ICONSET/icon_512x512@2x.png"

iconutil -c icns "$ICONSET" -o "$ICNS"
echo "OK: $ICNS"
```

- [ ] **Step 2: 실행 권한 부여 + 실행**

```bash
chmod +x Scripts/make-icon.sh
Scripts/make-icon.sh
```
Expected: `OK: Resources/AppIcon.icns` 출력, 오류 없음.

- [ ] **Step 3: 산출물 검증**

```bash
sips -g pixelWidth -g pixelHeight Resources/AppIcon.icns
file Resources/AppIcon.icns
```
Expected: `Resources/AppIcon.icns: Mac OS X icon` (file), sips가 크기 출력(대표 1024). 오류 없으면 통과.

- [ ] **Step 4: 커밋**

```bash
git add Scripts/make-icon.sh Resources/AppIcon.icns
git commit -m "feat: 아이콘 빌드 스크립트 + AppIcon.icns 생성"
```

---

### Task 3: 번들 통합 (Info.plist + bundle.sh)

**Files:**
- Modify: `Resources/Info.plist` (아이콘 키 추가)
- Modify: `Scripts/bundle.sh:24` 부근 (리소스 복사 영역)

- [ ] **Step 1: Info.plist에 CFBundleIconFile 추가**

`Resources/Info.plist`의 `<dict>` 안에 (예: `CFBundleName` 다음) 추가:

```xml
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
```

- [ ] **Step 2: bundle.sh에 아이콘 복사 스텝 추가**

`Scripts/bundle.sh`에서 `cp Resources/Info.plist "$APP/Contents/Info.plist"` 줄(현재 12행) **바로 다음**에 아이콘 복사를 추가한다. `mkdir -p "$APP/Contents/Resources"`는 현재 24행에 있으므로, 아이콘 복사는 그 mkdir 다음 위치가 자연스럽다. 아래 블록을 현재 24행 `mkdir -p "$APP/Contents/Resources"` 직후에 삽입:

```bash
# 앱 아이콘 (codesign 전에 배치해야 서명에 포함된다). 없으면 경고만 (아이콘 없이도 동작).
if [ -f Resources/AppIcon.icns ]; then
    cp Resources/AppIcon.icns "$APP/Contents/Resources/AppIcon.icns"
else
    echo "WARN: Resources/AppIcon.icns 없음 — 기본 아이콘으로 빌드됩니다" >&2
fi
```

- [ ] **Step 3: 번들 빌드 + 아이콘 포함/서명 검증**

```bash
Scripts/bundle.sh release
test -f build/SnapScreen.app/Contents/Resources/AppIcon.icns && echo "ICON OK" || echo "ICON MISSING"
codesign --verify --strict build/SnapScreen.app && echo "CODESIGN OK"
/usr/libexec/PlistBuddy -c "Print :CFBundleIconFile" build/SnapScreen.app/Contents/Info.plist
```
Expected: `OK: build/SnapScreen.app`, `ICON OK`, `CODESIGN OK`, `AppIcon`.

- [ ] **Step 4: Finder 아이콘 캐시 갱신 후 육안 확인(선택, 수동)**

```bash
open build/
```
Finder에서 `SnapScreen.app`이 새 아이콘으로 보이는지 확인. (아이콘 캐시로 즉시 안 바뀌면 `build/SnapScreen.app`을 다른 곳에 복사하거나 로그아웃/로그인 필요할 수 있음 — 최종 확인은 Task 4의 수동 체크리스트에서.)

- [ ] **Step 5: 커밋**

```bash
git add Resources/Info.plist Scripts/bundle.sh
git commit -m "feat: 앱 아이콘 번들 통합 (Info.plist + bundle.sh)"
```

---

### Task 4: 버전 범프 v0.6.0 + 문서 + PR

**Files:**
- Modify: `Sources/SnapScreenKit/Support/AppInfo.swift`
- Modify: `Resources/Info.plist`
- Modify: `docs/manual-test-checklist.md`, `README.md`, `CLAUDE.md`

- [ ] **Step 1: 버전 범프**

- `Sources/SnapScreenKit/Support/AppInfo.swift`: `version = "0.6.0"`
- `Resources/Info.plist`:
```bash
/usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 0.6.0" Resources/Info.plist
/usr/libexec/PlistBuddy -c "Set :CFBundleVersion 8" Resources/Info.plist
```

- [ ] **Step 2: 체크리스트에 "14. 앱 아이콘" 추가**

`docs/manual-test-checklist.md` 끝에:

```markdown
## 14. 앱 아이콘

- [ ] Finder에서 SnapScreen.app이 캡처 스택(바이올렛) 아이콘으로 보인다
- [ ] Dock에 앱 창을 띄웠을 때 Dock 아이콘이 새 아이콘이다
- [ ] ⌘Tab 앱 전환기에서 새 아이콘이 보인다
- [ ] 시스템 설정 > 개인정보 보호 > 화면 기록 목록에서 새 아이콘이 보인다
- [ ] 작은 크기(목록/메뉴)와 큰 크기 모두 형태가 또렷하다
```

- [ ] **Step 3: README 갱신**

`README.md`를 Read한 뒤, 앱 소개/스크린샷 영역에 아이콘 언급 또는 이미지를 추가한다(위치는 파일 구조에 맞게). 최소: 기능/소개 문단 근처에 "전용 앱 아이콘" 한 줄. 큰 변경 불필요.

- [ ] **Step 4: CLAUDE.md 갱신**

`CLAUDE.md`의 명령어 섹션에 한 줄 추가:

```markdown
Scripts/make-icon.sh                 # AppIcon.svg → Resources/AppIcon.icns 재생성
```

그리고 아키텍처/리소스 설명에 한 줄: 앱 아이콘은 `Resources/AppIcon.svg`가 소스이며 `make-icon.sh`로 `.icns`를 만들고 `bundle.sh`가 번들에 복사한다.

- [ ] **Step 5: 최종 검증**

```bash
swift test                                            # 48 PASS
rm -rf .build && swift build 2>&1 | grep -ci warning  # 0
Scripts/bundle.sh release                             # OK + ICON
file -I README.md docs/manual-test-checklist.md CLAUDE.md Resources/AppIcon.svg  # utf-8 (svg는 xml/svg)
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" Resources/Info.plist  # 0.6.0
/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" Resources/Info.plist              # 8
```

- [ ] **Step 6: Commit + Push + PR**

```bash
git add Sources/ Resources/ docs/ README.md CLAUDE.md
git commit -m "chore: 앱 아이콘 문서 + v0.6.0 버전 범프"
git push -u origin feat/app-icon
gh pr create --title "feat: 앱 아이콘 (캡처 스택)" --body "$(cat <<'EOF'
## Summary
- SnapScreen 전용 앱 아이콘 추가 (캡처 스택 모티프, 바이올렛 그라디언트, 캡처 프레임 심볼)
- `Resources/AppIcon.svg` 벡터 소스 + `Scripts/make-icon.sh`(SVG→iconset→.icns) + `Resources/AppIcon.icns`
- `Info.plist` CFBundleIconFile + `bundle.sh` 아이콘 복사(codesign 전)로 번들 통합
- v0.6.0 범프

## 설계/계획
- Spec: `docs/superpowers/specs/2026-07-06-app-icon-design.md`
- Plan: `docs/superpowers/plans/2026-07-06-app-icon.md`

## Test plan
- [x] `make-icon.sh` → `.icns` 생성, `swift test` 48개 통과, clean 빌드 경고 0
- [x] `bundle.sh` 후 번들에 AppIcon.icns 포함 + codesign 통과
- [ ] 수동: Finder/Dock/⌘Tab/시스템 설정에서 새 아이콘 표시, 크고 작은 크기 가독성 (checklist §14)
EOF
)"
```

커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 추가.

## 실행 순서와 검증 한계

- Task 1(SVG) → 2(.icns) → 3(번들 통합) → 4(버전·문서·PR). 순차 의존.
- 아이콘은 시각물이라 자동 단위 테스트 없음. 기계 검증은 PNG/`.icns` 생성·`codesign` 통과·번들 포함 여부. 최종 외형은 수동 §14.
- 릴리스(`make release VERSION=v0.6.0`)는 PR 머지 후 별도.
