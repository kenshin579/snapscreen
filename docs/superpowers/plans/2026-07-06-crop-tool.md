# Crop(자르기) 도구 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 편집기에서 주석이 없을 때 이미지를 드래그로 잘라내는 crop 도구 추가 (v0.5.0)

**Architecture:** `ImageCropper`(순수 함수, 좌표 변환+cropping+클램프)로 crop을 수행. crop은 주석도 EditorTool도 아닌 CanvasView의 별도 모드. ToolbarView에 세그먼트와 분리된 crop 버튼(주석 있으면 비활성 — AnnotationStore를 ObservableObject로 만들어 실시간 반영). 확정 시 EditorWindowController가 이미지를 교체하고 창을 리사이즈.

**Tech Stack:** Swift, CoreGraphics(CGImage.cropping), Combine(ObservableObject), 기존 SnapScreenKit Editor

**전제:**
- 브랜치 `feature/crop-tool`에서 작업 (설계 문서 커밋 있음). main 커밋 금지
- 커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 컨벤션: UI 클래스 `@MainActor`, 로직 계층 AppKit 비의존 단위 테스트, 한글 파일 `file -I` UTF-8 확인, clean 빌드 경고 0
- **좌표 규약**: Annotation·crop rect는 이미지 픽셀 **좌하단** 원점. `CGImage.cropping(to:)`은 데이터가 **좌상단** 원점 → y 뒤집기 필요

---

## 파일 구조 (완성 시점)

```
Sources/SnapScreenKit/Editor/
├── ImageCropper.swift          # crop 순수 함수 (신규, AppKit 비의존)
├── AnnotationStore.swift       # ObservableObject 전환 (수정)
├── CanvasView.swift            # crop 모드 + image var + replaceImage (수정)
├── ToolbarView.swift           # crop 버튼 (수정)
└── EditorWindowController.swift # crop 확정 → 이미지 교체 + 창 리사이즈 (수정)
Tests/SnapScreenKitTests/ImageCropperTests.swift
```

---

### Task 1: ImageCropper (TDD)

**Files:**
- Create: `Sources/SnapScreenKit/Editor/ImageCropper.swift`
- Test: `Tests/SnapScreenKitTests/ImageCropperTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

```swift
import XCTest
import CoreGraphics
@testable import SnapScreenKit

final class ImageCropperTests: XCTestCase {
    /// 좌하단 절반은 빨강, 상단 절반은 파랑인 이미지 (좌하단 원점 검증용)
    private func makeHalfImage(width: Int, height: Int) -> CGImage {
        let ctx = CGContext(data: nil, width: width, height: height,
                            bitsPerComponent: 8, bytesPerRow: 0,
                            space: CGColorSpace(name: CGColorSpace.sRGB)!,
                            bitmapInfo: CGImageAlphaInfo.premultipliedLast.rawValue)!
        // CGContext는 좌하단 원점: y=0..height/2 가 하단
        ctx.setFillColor(CGColor(red: 1, green: 0, blue: 0, alpha: 1))
        ctx.fill(CGRect(x: 0, y: 0, width: width, height: height / 2))            // 하단 빨강
        ctx.setFillColor(CGColor(red: 0, green: 0, blue: 1, alpha: 1))
        ctx.fill(CGRect(x: 0, y: height / 2, width: width, height: height / 2))   // 상단 파랑
        return ctx.makeImage()!
    }

    /// 이미지의 (x,y) 픽셀 색을 [R,G,B,A]로 (좌하단 원점 — CGContext에 그려서 샘플)
    private func pixel(_ image: CGImage, x: Int, y: Int) -> [UInt8] {
        var data = [UInt8](repeating: 0, count: 4)
        let ctx = CGContext(data: &data, width: 1, height: 1, bitsPerComponent: 8,
                            bytesPerRow: 4, space: CGColorSpace(name: CGColorSpace.sRGB)!,
                            bitmapInfo: CGImageAlphaInfo.premultipliedLast.rawValue)!
        ctx.draw(image, in: CGRect(x: -CGFloat(x), y: -CGFloat(y),
                                   width: CGFloat(image.width), height: CGFloat(image.height)))
        return data
    }

    func testCropSize() {
        let base = makeHalfImage(width: 200, height: 100)
        let out = ImageCropper.crop(base, toBottomLeftRect: CGRect(x: 10, y: 20, width: 50, height: 30))
        XCTAssertEqual(out?.width, 50)
        XCTAssertEqual(out?.height, 30)
    }

    func testCropBottomHalfIsRed() {
        // 좌하단 원점 rect가 하단(빨강) 영역을 정확히 집어내는지 — y 뒤집기 검증
        let base = makeHalfImage(width: 200, height: 100)
        let out = ImageCropper.crop(base, toBottomLeftRect: CGRect(x: 0, y: 0, width: 200, height: 40))!
        let p = pixel(out, x: 100, y: 20)   // crop 결과의 중앙
        XCTAssertGreaterThan(p[0], 200)     // 빨강
        XCTAssertLessThan(p[2], 50)         // 파랑 아님
    }

    func testCropTopHalfIsBlue() {
        let base = makeHalfImage(width: 200, height: 100)
        let out = ImageCropper.crop(base, toBottomLeftRect: CGRect(x: 0, y: 60, width: 200, height: 40))!
        let p = pixel(out, x: 100, y: 20)
        XCTAssertGreaterThan(p[2], 200)     // 파랑
        XCTAssertLessThan(p[0], 50)         // 빨강 아님
    }

    func testClampsToBounds() {
        // 이미지 밖으로 나간 rect는 경계로 클램프
        let base = makeHalfImage(width: 200, height: 100)
        let out = ImageCropper.crop(base, toBottomLeftRect: CGRect(x: 180, y: 90, width: 100, height: 100))
        XCTAssertEqual(out?.width, 20)   // 200-180
        XCTAssertEqual(out?.height, 10)  // 100-90
    }

    func testEmptyRectReturnsNil() {
        let base = makeHalfImage(width: 200, height: 100)
        XCTAssertNil(ImageCropper.crop(base, toBottomLeftRect: CGRect(x: 300, y: 300, width: 10, height: 10)))
        XCTAssertNil(ImageCropper.crop(base, toBottomLeftRect: .zero))
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `swift test --filter ImageCropperTests`
Expected: 컴파일 에러 — `cannot find 'ImageCropper'`

- [ ] **Step 3: 구현** — `ImageCropper.swift`

```swift
import CoreGraphics

/// 이미지 자르기. rect는 이미지 픽셀 좌표(원점 좌하단, 코드베이스 규약).
/// CGImage.cropping(to:)은 데이터가 좌상단 원점이라 y를 뒤집는다.
public enum ImageCropper {
    public static func crop(_ image: CGImage, toBottomLeftRect rect: CGRect) -> CGImage? {
        let bounds = CGRect(x: 0, y: 0, width: image.width, height: image.height)
        let clamped = rect.integral.intersection(bounds)
        guard !clamped.isNull, clamped.width >= 1, clamped.height >= 1 else { return nil }
        // 좌하단 → 좌상단 y 뒤집기
        let topLeft = CGRect(x: clamped.minX,
                             y: CGFloat(image.height) - clamped.maxY,
                             width: clamped.width,
                             height: clamped.height)
        return image.cropping(to: topLeft)
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `swift test`
Expected: 전부 PASS (기존 43 + 신규 5 = 48개)

- [ ] **Step 5: Commit**

```bash
git add Sources/SnapScreenKit/Editor/ImageCropper.swift Tests/
git commit -m "feat: 이미지 크롭 순수 함수 (좌하단→좌상단 좌표 변환 + 경계 클램프)"
```

---

### Task 2: AnnotationStore 관찰 가능화

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/AnnotationStore.swift`

- [ ] **Step 1: 현재 파일 Read 후 ObservableObject 전환**

`AnnotationStore.swift`를 Read한다. 클래스 선언을 `ObservableObject`로 바꾸고 `annotations`를 `@Published`로 만든다. import에 `Combine` 추가(또는 Foundation로 충분하면 유지). 예:

```swift
import Foundation
import Combine

public final class AnnotationStore: ObservableObject {
    @Published public private(set) var annotations: [Annotation] = []
    // ... 기존 undo/redo 스택, add/remove/translate/undo/redo, nextStepNumber 등 전부 그대로 유지
```

기존 메서드들은 `annotations`에 대입/변경하므로 `@Published`가 자동 발화한다. **로직은 변경하지 않는다** — 선언만 `@Published`로, 클래스만 `ObservableObject`로.

- [ ] **Step 2: 빌드 + 테스트**

Run: `swift build && swift test`
Expected: 43개 유지(신규 없음). 만약 `AnnotationStoreTests`가 컴파일 에러(`@Published` 접근이나 actor 격리)나면 최소 조정:
- ObservableObject만으로 컴파일 안 되는 경우는 거의 없다. `@Published private(set)`는 외부 읽기 그대로라 테스트 영향 없음.
- 혹시 Swift 6 격리 경고가 뜨면 `AnnotationStore`에 `@MainActor`를 붙이고 `AnnotationStoreTests`/`AnnotationHitTesterTests`의 관련 메서드에 `@MainActor`를 붙여 해소(편차 보고).

Run: `rm -rf .build && swift build 2>&1 | grep -ci warning`
Expected: 0

- [ ] **Step 3: Commit**

```bash
git add Sources/SnapScreenKit/Editor/AnnotationStore.swift Tests/
git commit -m "refactor: AnnotationStore를 ObservableObject로 (crop 버튼 실시간 비활성용)"
```

---

### Task 3: CanvasView crop 모드

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/CanvasView.swift`

- [ ] **Step 1: image를 var로 + replaceImage 추가**

`CanvasView`의 `let image: CGImage`를 `var image: CGImage`로 바꾸고, 메서드 추가 (fitScale/fitOffset은 computed라 자동 갱신):

```swift
    /// crop 등으로 캔버스 이미지를 교체
    func replaceImage(_ newImage: CGImage) {
        image = newImage
        needsDisplay = true
    }
```

- [ ] **Step 2: crop 상태 + 진입/종료 + 확정 콜백 추가**

`CanvasView`에 프로퍼티 추가 (기존 dragMode 근처):

```swift
    // crop 모드 (주석 아님 — 이미지 픽셀 좌하단 좌표)
    private var isCropping = false
    private var cropStart: CGPoint?
    private var cropRect: CGRect?
    private var cropConfirmButton: NSButton?
    private var cropCancelButton: NSButton?
    /// 확정된 crop 영역(이미지 픽셀 좌표)을 컨트롤러에 전달
    var onCropConfirmed: ((CGRect) -> Void)?

    func beginCrop() {
        commitTextFieldIfNeeded()
        selectedID = nil
        isCropping = true
        cropStart = nil
        cropRect = nil
        removeCropButtons()
        window?.makeFirstResponder(self)
        needsDisplay = true
    }

    private func endCrop() {
        isCropping = false
        cropStart = nil
        cropRect = nil
        removeCropButtons()
        needsDisplay = true
    }

    private func removeCropButtons() {
        cropConfirmButton?.removeFromSuperview(); cropConfirmButton = nil
        cropCancelButton?.removeFromSuperview(); cropCancelButton = nil
    }

    private func confirmCrop() {
        guard let rect = cropRect, rect.width >= 8, rect.height >= 8 else { return }
        let confirmed = rect
        endCrop()
        onCropConfirmed?(confirmed)
    }
```

- [ ] **Step 3: 마우스 핸들러에 crop 분기 추가**

`mouseDown`의 맨 앞(commitTextFieldIfNeeded 다음)에 crop 분기 추가:

```swift
    public override func mouseDown(with event: NSEvent) {
        commitTextFieldIfNeeded()
        let p = imagePoint(from: event)
        if isCropping {
            cropStart = clampToImage(p)
            cropRect = nil
            removeCropButtons()
            needsDisplay = true
            return
        }
        // 레터박스(이미지 밖) 클릭은 무시 — flatten 시 유실될 주석 생성 방지
        guard (0...CGFloat(image.width)).contains(p.x),
              (0...CGFloat(image.height)).contains(p.y) else { return }
        // ... 기존 주석 로직 그대로
```

`mouseDragged`의 맨 앞에 crop 분기:

```swift
    public override func mouseDragged(with event: NSEvent) {
        if isCropping, let start = cropStart {
            let p = clampToImage(imagePoint(from: event))
            cropRect = CGRect(x: min(start.x, p.x), y: min(start.y, p.y),
                              width: abs(start.x - p.x), height: abs(start.y - p.y))
            needsDisplay = true
            return
        }
        // ... 기존 주석 로직 그대로
```

`mouseUp`의 맨 앞에 crop 분기 (드래그 끝나면 확정/취소 버튼 표시):

```swift
    public override func mouseUp(with event: NSEvent) {
        if isCropping {
            cropStart = nil
            if let rect = cropRect, rect.width >= 8, rect.height >= 8 {
                showCropButtons(for: rect)
            } else {
                cropRect = nil
            }
            needsDisplay = true
            return
        }
        // ... 기존 주석 로직 그대로
```

`clampToImage` 헬퍼 추가 (imagePoint 근처):

```swift
    private func clampToImage(_ p: CGPoint) -> CGPoint {
        CGPoint(x: min(max(0, p.x), CGFloat(image.width)),
                y: min(max(0, p.y), CGFloat(image.height)))
    }
```

- [ ] **Step 4: 확정/취소 버튼 표시 (선택 영역 우하단)**

```swift
    /// crop rect(이미지 픽셀 좌표)를 뷰 좌표로 변환한 우하단 근처에 ✓/✗ 버튼 배치
    private func showCropButtons(for rect: CGRect) {
        removeCropButtons()
        // 이미지 픽셀 → 뷰 좌표
        let viewMaxX = fitOffset.x + rect.maxX * fitScale
        let viewMinY = fitOffset.y + rect.minY * fitScale
        let size: CGFloat = 28
        let gap: CGFloat = 6

        let confirm = NSButton(frame: CGRect(x: viewMaxX - size * 2 - gap,
                                             y: viewMinY + gap, width: size, height: size))
        confirm.bezelStyle = .circular
        confirm.image = NSImage(systemSymbolName: "checkmark", accessibilityDescription: "자르기 확정")
        confirm.target = self
        confirm.action = #selector(cropConfirmClicked)
        confirm.toolTip = "자르기 (⏎)"

        let cancel = NSButton(frame: CGRect(x: viewMaxX - size, y: viewMinY + gap,
                                            width: size, height: size))
        cancel.bezelStyle = .circular
        cancel.image = NSImage(systemSymbolName: "xmark", accessibilityDescription: "자르기 취소")
        cancel.target = self
        cancel.action = #selector(cropCancelClicked)
        cancel.toolTip = "취소 (esc)"

        addSubview(confirm); addSubview(cancel)
        cropConfirmButton = confirm; cropCancelButton = cancel
    }

    @objc private func cropConfirmClicked() { confirmCrop() }
    @objc private func cropCancelClicked() { endCrop() }
```

- [ ] **Step 5: draw에 crop dimming/테두리 추가**

`drawOverlays`의 끝(선택 표시 다음)에 crop 렌더 추가:

```swift
    func drawOverlays(in ctx: CGContext) {
        // ... 기존 draft/selection 렌더 그대로 ...

        if isCropping {
            let imageRect = CGRect(x: 0, y: 0, width: image.width, height: image.height)
            ctx.setFillColor(NSColor(white: 0, alpha: 0.4).cgColor)
            if let rect = cropRect {
                // 선택 영역만 뚫고 나머지 어둡게 (even-odd)
                let path = CGMutablePath()
                path.addRect(imageRect)
                path.addRect(rect)
                ctx.addPath(path)
                ctx.fillPath(using: .evenOdd)
                ctx.setStrokeColor(NSColor.controlAccentColor.cgColor)
                ctx.setLineWidth(1.0 / fitScale)
                ctx.stroke(rect)
            } else {
                ctx.fill(imageRect) // 아직 안 그렸으면 전체 어둡게
            }
        }
    }
```

- [ ] **Step 6: keyDown에 Enter(확정)/esc(취소) + 단축키 C**

`keyDown`의 맨 앞에 crop 모드 분기 추가:

```swift
    public override func keyDown(with event: NSEvent) {
        if isCropping {
            switch event.keyCode {
            case 36, 76: confirmCrop(); return   // return, enter
            case 53: endCrop(); return            // esc
            default: break
            }
        }
        switch event.keyCode {
        // ... 기존 delete/esc/도구 단축키 ...
```

기존 도구 단축키 mapping에는 crop을 넣지 않는다(EditorTool 아님). 대신 `c` 키를 crop 진입으로 처리하려면 mapping default 분기에서 별도 처리 — 단, crop은 주석 없을 때만 유효하므로 `c` 입력 시 `store.annotations.isEmpty`면 `beginCrop()`:

기존 mapping 처리 블록에서, mapping에 매칭 안 될 때 `c` 확인 추가:

```swift
            if let tool = mapping[char] {
                state.tool = tool
            } else if char == "c", store.annotations.isEmpty {
                beginCrop()
            } else {
                super.keyDown(with: event)
            }
```

- [ ] **Step 7: 빌드 + 테스트 + 상주 확인**

Run: `rm -rf .build && swift build && swift test`
Expected: 빌드 성공(경고 0), 48개 PASS

이어서 `Scripts/run.sh` → `pgrep -x SnapScreen` → `pkill -x SnapScreen`. 육안 확인 항목은 보고서에 명시(crop 자체는 Task 4의 툴바 버튼 연결 후 완전 동작).

- [ ] **Step 8: Commit**

```bash
git add Sources/SnapScreenKit/Editor/CanvasView.swift
git commit -m "feat: 캔버스 crop 모드 (드래그·dimming·확정/취소 버튼·Enter/esc)"
```

---

### Task 4: ToolbarView crop 버튼 + EditorWindowController 통합

**Files:**
- Modify: `Sources/SnapScreenKit/Editor/ToolbarView.swift`
- Modify: `Sources/SnapScreenKit/Editor/EditorWindowController.swift`

- [ ] **Step 1: ToolbarView에 store + onCrop 추가**

`ToolbarView`에 프로퍼티/init 파라미터 추가:

```swift
    @ObservedObject var state: EditorState
    @ObservedObject var store: AnnotationStore
    let onCrop: () -> Void
    let onUndo: () -> Void
    // ... 나머지 기존 클로저

    public init(state: EditorState, store: AnnotationStore,
                onCrop: @escaping () -> Void,
                onUndo: @escaping () -> Void, onRedo: @escaping () -> Void,
                onCopy: @escaping () -> Void, onSave: @escaping () -> Void) {
        self.state = state
        self.store = store
        self.onCrop = onCrop
        self.onUndo = onUndo
        self.onRedo = onRedo
        self.onCopy = onCopy
        self.onSave = onSave
    }
```

body의 도구 Picker 다음 `Divider().frame(height: 20)` **뒤**(색상 팔레트 앞)에 crop 버튼 추가:

```swift
            Button(action: onCrop) { Image(systemName: "crop") }
                .help(store.annotations.isEmpty
                      ? "자르기 (C)" : "주석을 모두 삭제한 후 자를 수 있습니다")
                .disabled(!store.annotations.isEmpty)

            Divider().frame(height: 20)
```

(색상 팔레트 앞에 crop 버튼 + Divider를 넣어 도구군과 색상군을 구분.)

- [ ] **Step 2: EditorWindowController — 이미지 단일 소스 + crop 배선**

`EditorWindowController`를 Read한다. 현재 `private let result: CaptureResult`에서 이미지를 참조하는 부분(canvas 생성, flatten의 base)을 `private var image: CGImage`로 단일화한다.

프로퍼티 추가/변경:

```swift
    private var image: CGImage        // 현재 이미지 (crop으로 교체됨)
    // result.scale은 그대로 사용 (crop은 배율 불변)
```

init에서 `self.image = result.image` 초기화(기존 `result` 저장은 유지하거나 image/scale만 보관). canvas 생성 시 `image:`에 `self.image` 전달(현재 `result.image`였다면 변경). `flatten()`의 base를 `result.image` → `self.image`로 변경.

ToolbarView 생성부에 store와 onCrop 전달:

```swift
        let toolbar = NSHostingView(rootView: ToolbarView(
            state: state,
            store: store,
            onCrop: { [weak self] in self?.canvas.beginCrop() },
            onUndo: { [weak self] in self?.undoAction(nil) },
            onRedo: { [weak self] in self?.redoAction(nil) },
            onCopy: { [weak self] in self?.copyMerged(nil) },
            onSave: { [weak self] in self?.saveImage(nil) }
        ))
```

canvas 생성 다음에 crop 확정 콜백 배선:

```swift
        canvas.onCropConfirmed = { [weak self] rect in
            self?.applyCrop(rect)
        }
```

crop 적용 + 창 리사이즈 메서드 추가:

```swift
    private func applyCrop(_ rect: CGRect) {
        guard let cropped = ImageCropper.crop(image, toBottomLeftRect: rect) else { return }
        image = cropped
        canvas.replaceImage(cropped)
        resizeWindowToImage()
    }

    /// 현재 이미지 비율에 맞게 창 content 크기 재조정 (init 사이징 로직과 동일 규칙)
    private func resizeWindowToImage() {
        guard let window else { return }
        let pointSize = CGSize(width: CGFloat(image.width) / result.scale,
                               height: CGFloat(image.height) / result.scale)
        let maxSize = NSScreen.main.map { CGSize(width: $0.visibleFrame.width * 0.8,
                                                 height: $0.visibleFrame.height * 0.8) }
            ?? CGSize(width: 1200, height: 800)
        let fit = min(1, maxSize.width / pointSize.width, maxSize.height / pointSize.height)
        let contentSize = CGSize(width: pointSize.width * fit, height: pointSize.height * fit)
        let toolbarHeight: CGFloat = 44
        window.setContentSize(CGSize(width: contentSize.width,
                                     height: contentSize.height + toolbarHeight))
    }
```

(주의: `result.scale`을 계속 쓰므로 `result` 저장은 유지한다. `image`만 crop으로 교체되는 별도 프로퍼티다. init의 초기 사이징 코드도 이 `resizeWindowToImage`와 규칙이 같으니, 원하면 init에서도 이 헬퍼를 재사용해 중복 제거 가능 — 하되 init 시점엔 window/canvas가 준비된 후 호출해야 함.)

- [ ] **Step 3: 빌드 + 테스트 + 상주 확인**

Run: `rm -rf .build && swift build && swift test`
Expected: 빌드 성공(경고 0), 48개 PASS

`Scripts/run.sh` → `pgrep` → `pkill`. 육안 확인 항목 보고: crop 버튼(주석 없을 때 활성/있을 때 회색+툴팁), 드래그→dimming→✓/✗ 버튼, Enter/esc, 확정 후 이미지·창 크기 변경, crop 후 주석 그리기, ⌘S/⌘C에 crop 반영, `C` 단축키.

- [ ] **Step 4: Commit**

```bash
git add Sources/SnapScreenKit/Editor/
git commit -m "feat: crop 버튼 배선 + 확정 시 이미지 교체·창 리사이즈"
```

---

### Task 5: 문서 + v0.5.0 범프 + PR

**Files:**
- Modify: `docs/manual-test-checklist.md`
- Modify: `README.md`
- Modify: `CLAUDE.md`
- Modify: `Sources/SnapScreenKit/Support/AppInfo.swift`
- Modify: `Resources/Info.plist`

- [ ] **Step 1: 체크리스트에 "13. 자르기" 섹션 추가** — `docs/manual-test-checklist.md` 끝에:

```markdown
## 13. 자르기 (Crop)

- [ ] 주석이 없을 때 툴바의 자르기 버튼이 활성화되어 있다
- [ ] 주석이 하나라도 있으면 자르기 버튼이 비활성(회색)이고 툴팁에 안내가 표시된다
- [ ] 자르기 버튼(또는 C 키)을 누르면 crop 모드로 들어가 화면이 어두워진다
- [ ] 드래그로 영역을 지정하면 그 부분만 밝아지고 테두리가 표시된다
- [ ] 드래그 후 선택 영역 근처에 확정(✓)/취소(✗) 버튼이 나타난다
- [ ] ✓ 또는 Enter로 확정하면 이미지가 그 영역으로 잘리고 창 크기가 새 비율에 맞게 바뀐다
- [ ] ✗ 또는 esc로 취소하면 이미지가 그대로 유지된다
- [ ] 자른 뒤 이어서 주석을 그릴 수 있다
- [ ] 자른 뒤 ⌘C/⌘S로 내보내면 잘린 이미지가 반영된다
```

- [ ] **Step 2: README 갱신** — 주석 도구/편집기 기능 설명에 "자르기(crop)"를 추가한다 (실제 파일 Read 후 맞는 위치에).

- [ ] **Step 3: CLAUDE.md 갱신** — Editor 모듈 설명에 crop 관련 한 줄 추가 (실제 파일 Read 후):

```markdown
crop은 주석/도구가 아닌 CanvasView의 별도 모드다. `ImageCropper`(순수 함수)가 좌하단→좌상단 좌표 변환 후 자르며, 확정 시 EditorWindowController가 이미지를 교체하고 창을 리사이즈한다. crop 버튼 비활성을 위해 `AnnotationStore`는 `ObservableObject`다.
```

- [ ] **Step 4: 버전 범프**

- `AppInfo.swift`: `version = "0.5.0"`
- `Resources/Info.plist`: `CFBundleShortVersionString` → `0.5.0`, `CFBundleVersion` → `6`

- [ ] **Step 5: 최종 검증**

```bash
swift test                        # 48개 PASS
rm -rf .build && swift build 2>&1 | grep -ci warning   # 0
Scripts/bundle.sh release         # OK
file -I README.md docs/manual-test-checklist.md CLAUDE.md   # utf-8
/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" Resources/Info.plist  # 0.5.0
```

- [ ] **Step 6: Commit + Push + PR**

```bash
git add docs/ README.md CLAUDE.md Sources/ Resources/
git commit -m "chore: crop 도구 문서 + v0.5.0 버전 범프"
git push -u origin feature/crop-tool
gh pr create --title "feat: 자르기(crop) 도구" --body "$(cat <<'EOF'
## Summary
- 편집기에서 이미지를 드래그로 잘라내는 crop 도구 (주석 없을 때만, 단축키 C)
- 툴바에 도구 세그먼트와 분리된 crop 버튼 — 주석이 있으면 비활성(SwiftUI 세그먼트 개별 비활성 불가 회피 + 의미적 분리)
- 드래그 → dimming 미리보기 → 확정(✓)/취소(✗) 버튼 + Enter/esc
- 확정 시 같은 창에서 이미지 교체 + 창 리사이즈, 이어서 주석 작업 가능, ⌘C/⌘S에 반영
- `ImageCropper` 순수 함수(좌하단→좌상단 좌표 변환 + 경계 클램프, 단위 테스트 5개)
- crop 버튼 실시간 비활성을 위해 AnnotationStore를 ObservableObject로 전환
- v0.5.0 범프

## 설계/계획
- Spec: `docs/superpowers/specs/2026-07-06-crop-tool-design.md`
- Plan: `docs/superpowers/plans/2026-07-06-crop-tool.md`

## Test plan
- [x] `swift test` 48개 통과 (ImageCropper 5개 포함), clean 빌드 경고 0
- [ ] 수동: crop 버튼 활성/비활성, 드래그·dimming·✓/✗·Enter/esc, 확정 후 이미지·창 크기, crop 후 주석·저장 반영 (checklist §13)
EOF
)"
```

커밋 메시지 끝에 빈 줄 후 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 추가.

## 실행 순서와 검증 한계

- Task 1(크롭 함수) → 2(store 관찰) → 3(캔버스 crop 모드) → 4(툴바 버튼+통합) → 5(문서·범프·PR). 순차 의존(4가 1·2·3 전부 사용).
- crop의 실제 드래그·dimming·버튼·창 리사이즈는 GUI라 자동 테스트 불가 → 수동 §13. 단위 테스트는 `ImageCropper`의 좌표 변환·클램프만 커버.
- 릴리스(`make release VERSION=v0.5.0`)는 PR 머지 후 별도.
